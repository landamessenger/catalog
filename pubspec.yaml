name: catalog
description: A Flutter package to render widgets in real time and generate screenshots for fastlane.
version: 1.0.4
homepage: https://landamessenger.com/
repository: https://github.com/landamessenger/catalog

environment:
  sdk: '>=2.19.6 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  blurhash_dart: ^1.2.1                         # android   ios   linux   macos         windows
  device_frame: ^1.1.0                          # android   ios   linux   macos   web   windows
  flutter_svg: ^2.0.9                           # android   ios   linux   macos         windows
  global_refresh: ^1.0.0                        # android   ios   linux   macos   web   windows
  go_router: ^13.2.0                            # android   ios   linux   macos   web   windows
  http: ^1.1.2                                  # android   ios   linux   macos   web   windows
  image: ^4.1.7                                 # android   ios   linux   macos   web   windows
  shelf: ^1.4.1                                 # android   ios   linux   macos   web   windows
  vector_graphics: ^1.1.10+1                    # android   ios   linux   macos   web   windows
  yaml: ^3.1.2                                  # android   ios   linux   macos   web   windows
  yaml_writer: ^2.0.0                           # android   ios   linux   macos   web   windows

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
