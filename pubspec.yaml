name: catalog
description: A Flutter package for render widgets in real-time
version: 0.1.0
homepage: https://landamessenger.com/

environment:
  sdk: '>=2.19.6 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  device_frame: ^1.1.0                          # android   ios   linux   macos   web   windows
  flutter_svg: ^2.0.9                           # android   ios   linux   macos         windows
  go_router: ^13.0.1                            # android   ios   linux   macos   web   windows
  http: ^1.1.2                                  # android   ios   linux   macos   web   windows
  vector_graphics: ^1.1.9+2                     # android   ios   linux   macos   web   windows
  yaml: ^3.1.2                                  # android   ios   linux   macos   web   windows
  yaml_writer: ^1.0.3                           # android   ios   linux   macos   web   windows

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
